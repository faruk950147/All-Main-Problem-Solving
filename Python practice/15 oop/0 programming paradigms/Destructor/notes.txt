Destructor হলো একটি বিশেষ মেথড যেটি কোনো object destroy (বা মেমোরি থেকে remove) হওয়ার সময় অটোমেটিক কল হয়। অর্থাৎ, যখন কোনো অবজেক্টের লাইফটাইম শেষ হয় (মানে যখন অবজেক্ট আর ব্যবহার হবে না), তখন destructor চালু হয় এবং এটি সাধারণত ক্লিনআপ কাজের জন্য ব্যবহৃত হয় — যেমন: ফাইল বন্ধ করা, মেমোরি রিলিজ করা ইত্যাদি।

🔹 Destructor-এর বৈশিষ্ট্য:
এটি অটোমেটিক কল হয় যখন অবজেক্ট ধ্বংস (delete) হয়।

এর নাম সাধারণত __del__() (Python এ)।

C++/Java/C#-এ destructor-এর syntax ভিন্ন হয়।

🔸 Python-এ Destructor এর উদাহরণ:
python
Copy
Edit
class Student:
    def __init__(self, name):
        self.name = name
        print(f"{self.name} created!")

    def __del__(self):
        print(f"{self.name} destroyed!")

# অবজেক্ট তৈরি
s1 = Student("Omar")

# অবজেক্ট destroy
del s1
আউটপুট:

nginx
Copy
Edit
Omar created!
Omar destroyed!
🔸 C++ এ Destructor:
cpp
Copy
Edit
#include <iostream>
using namespace std;

class Student {
public:
    Student() {
        cout << "Constructor called" << endl;
    }

    ~Student() {
        cout << "Destructor called" << endl;
    }
};

int main() {
    Student s1;  // scope এর শেষে destructor কল হবে
}
🔹 Destructor কবে দরকার হয়?
যখন আপনি dynamic resource (মেমোরি, ফাইল, নেটওয়ার্ক connection) ব্যবহার করছেন।

মেমোরি লিক (memory leak) রোধ করতে।

মনে রাখবেন:
Python-এর ক্ষেত্রে destructor কল হওয়ার সময় নির্ভর করে Garbage Collector কবে object কে মুছে ফেলছে তার উপর।

তবে C++ এ scope শেষ হলে destructor সঙ্গে সঙ্গে কল হয়।
🔧 Destructor কীভাবে কাজ করে? (Working of Destructor)

Step-by-step process:
1. Object Create হয়
প্রথমে কোনো ক্লাস থেকে একটি অবজেক্ট তৈরি হয়। এই সময় __init__() (Python) বা constructor কল হয়।

2. Object এর কাজ শেষ হয়
অবজেক্ট ব্যবহারের পর যখন তার আর দরকার নেই — তখন Python-এর Garbage Collector অথবা C++ এর scope determine করে কখন অবজেক্ট destroy হবে।

3. Destructor অটোমেটিক কল হয়
অবজেক্ট destroy হওয়ার ঠিক আগে destructor method অটোমেটিক কল হয়। এতে আপনি cleanup কাজ করতে পারেন।

4. Resource Free হয়ে যায়
যে কোনো ফাইল, মেমোরি, নেটওয়ার্ক resource যা অবজেক্ট ব্যবহার করছিল তা release হয়।

🔸 উদাহরণ (Python):
python
Copy
Edit
class FileHandler:
    def __init__(self, filename):
        self.file = open(filename, "w")
        print("File opened.")

    def write_data(self, data):
        self.file.write(data)

    def __del__(self):
        self.file.close()
        print("File closed (Destructor called).")

# ব্যবহার:
handler = FileHandler("data.txt")
handler.write_data("Hello, world!")

# অবজেক্ট delete
del handler
📌 এখানে __del__() destructor file close করে দিচ্ছে যখন handler object মুছে ফেলা হয়।

🔸 উদাহরণ (C++):
cpp
Copy
Edit
#include <iostream>
using namespace std;

class MyClass {
public:
    MyClass() {
        cout << "Constructor called" << endl;
    }

    ~MyClass() {
        cout << "Destructor called" << endl;
    }
};

int main() {
    MyClass obj;  // এখানে constructor কল হবে
    // main এর scope শেষ হলে destructor অটোমেটিক কল হবে
}
⚠️ মনে রাখবেন:
Python-এ destructor কখন কল হবে তা নির্ভর করে garbage collector-এর উপর।

C++-এ অবজেক্ট scope এর বাইরে গেলেই destructor সঙ্গে সঙ্গে কল হয়।

Destructor-এ এমন কাজ করবেন যা object destroy না হলে শেষ হবে না (ex: ফাইল বন্ধ, কানেকশন ক্লোজ, মেমোরি ফ্রি করা)।


